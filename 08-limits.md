# 边界与限制

以下每一条都是当前的实际行为，写代码时按它们来。

---

## 0. 速览

| 边界 | 说明 | 落到代码上的样子 |
| --- | --- | --- |
| [没有续跑](#1-没有续跑) | 让出之后接不回来 | 长任务只能靠调大 `yield_time_ms` |
| [没有取消](#2-没有取消) | 启动后无法从外部中止 | 程序失控时只能杀进程；调试期规模压到 1 |
| [不能问人](#3-不能问人) | 程序中途没法向人提问 | 需要判断的地方只能写成代码 |
| [等待调用串行](#4-等待调用串行并发不等于流式) | `Promise.race` 拿不到最先完成的 | 不能设计成流水线 |
| [没有结构化输出保证](#5-没有结构化输出保证) | prompt 里的格式要求只是要求 | 必须防御性解析 + 留原文 |
| [自带能力只到 stdio MCP](#6-自带能力只到-stdio-mcp) | 包能带自己的 MCP server，但带不了角色与提示词 | 能力写进 manifest；`message` 仍是唯一控制面 |
| [没有启动参数](#7-没有启动参数) | 提交接口不收参数，只收一个包 | 参数写死或打进包里，不能运行时传 |
| [并发有上限且两代不同](#8-并发上限两代语义不同) | v1 计累计派出数，v2 计同时驻留数 | 超额时必须分批，`concurrency` 守不住 |
| [v2 没有递归深度限制](#9-递归深度v2-下无条件放行) | 子 agent 能无限往下派 | 深度限制自行在 JS 里实现 |
| [运行时读不到工具 schema](#10-运行时读不到工具-schema) | `ALL_TOOLS` 只有名字和描述 | 参数只能查文档 |
| [重跑不可复现](#11-重跑不可复现) | 有真实时钟，没有可回放的记录 | 不能依赖两次运行结果一致 |

---

## 1. 没有续跑

程序中途让出（`yield_control()`）之后，已累积的输出会交出去，但后半段回不来，没有配套的手段去接。

模型侧有这个能力（它可以用一个专门的工具续跑一个让出的 cell），程序侧没有对应物。

写代码时的结论：

- `yield_control()` 不能当"流式输出"用
- 长程序的正解是把首行 pragma 的 `yield_time_ms` 调大，而不是分段
- 整个并行派发必须在这一次运行的超时内跑完

---

## 2. 没有取消

没有任何手段能中止一个正在跑的程序。程序失控时只能杀进程。

配合上一条（让出后无法续跑），调试成本很高。由此产生的习惯见《05-writing.md》§9：

- `yield_time_ms` 分两档写，调试期 `60000`
- 规模常量提到顶部，调试时全设成 1
- 观测点靠攒 `log` 数组，最后随结果一起交出去

---

## 3. 不能问人

**程序没有跟人对话的手段。**

内置的 `request_user_input` 有三道门，每一道单独就足以挡死：它挂在一个实验开关后面；注册时的曝光度是 `DirectModelOnly`，而这一档判定为不进 code mode；即便前两道都放开，它还会拒绝非根线程的调用。三道是并列的。

`codex exec` 另外把审批策略钉成 `never`，这一层压在 `-c approval_policy=...` 之上。

但"钉成 `never`"不等于"问人的请求会被快速拒掉"。在 PoA 下，任何走到"向人提问"这一步的调用都是无限挂死，与审批策略无关：审批回执要登记在一次模型回合上，而 cell 从不建立回合，回执无处投递，那次调用的 promise 永不落地。普通 turn 里观察到的"毫秒级自动 decline"是回合存在时的行为，不适用于 cell。

写代码时的结论：

- PoA 程序必须能在没有人的情况下从头跑到尾
- 需要人判断的地方，只能把判断也写成代码（比如《06-patterns.md》里交叉验证的数票），或者把问题留到最后的输出里
- 任何一次会触发询问或审批的调用都会把整条 RPC 挂死，`yield_time_ms` 兜不住，只能杀进程。最常见的触发源是没写 annotations 的 MCP 工具，见《10-packaging.md》§4.2

---

## 4. 等待调用串行（并发不等于流式）

工具执行时会取一把共享锁；声明了并行安全的取读锁（可共存），没声明的取写锁（独占）。子 agent 那两代工具一个都没有声明并行安全。

**agent 本身是真并发的**，三个同时执行，总耗时接近最慢那个而不是三者之和。串行的是程序侧的等待调用。

丢掉的具体是这三件事：

| 做不到 | 具体表现 |
| --- | --- |
| 流式输出 | 没法边收边处理 |
| 早停 | 拿到满意结果后没法提前结束剩下的 |
| 动态调度 | 等待期间连再派一个新 agent 都做不到 |

写代码时按"全部派出去 → 一次 join → 统一收口"组织。

唯一的做法是把耗时任务放进一个标了只读的 MCP server，那样它就是并行安全的（判据见《07-api-reference.md》§3.1）。

这条做法不需要宿主配合。包自带的 stdio server 也走同一条判据："工具自己声明并行安全，或带只读标注"。包这边走不通前半条（包起的 server 那份配置里 `supports_parallel_tool_calls` 固定为 `false`，服务器级的整体豁免用不了），所以只能逐个工具标只读。

`destructiveHint` 在这条判据里不参与，它管的是审批那条，那边多一道 `destructiveHint === true` 的短路。两条判据是独立的：`{readOnlyHint: true, destructiveHint: true}` 在并行判据下安全，在审批判据下仍需审批，而在 PoA 下需审批即挂死（§3）。三个标注一起写是审批那边的要求，写法见《10-packaging.md》§4.2。

---

## 5. 没有结构化输出保证

没有"内核校验 JSON schema、不合规就重试"的机制。prompt 里那句 `只回一行 JSON` 只是要求，不是保证。

写代码时的结论：

- 每个 agent 回复都过一次防御性解析（`parseJsonReply`），并处理 `error` 分支
- 解析失败时把原始回复截前 200 字留下来
- 需要重试时自己在 JS 里写重试循环，宿主不会代劳

`succeeded / total` 这个比值衡量的不是 agent 答得好不好，而是这套替代方案的可靠程度。

---

## 6. 自带能力只到 stdio MCP

程序可以自带能力：把它打成一个 `.poa` 包，在 `manifest.toml` 里写 `[[capabilities.mcp]]`，服务端解包后按 thread 起这些 server，随 thread 生死，不写全局 `config.toml`。写法见《10-packaging.md》文档。

这一条限制的不是"能不能自带"，而是"自带到什么程度"。当前的边界：

| 能自带 | 不能自带 |
| --- | --- |
| `stdio` 传输的 MCP server（进程直接派生，不经 shell） | `network` 传输——解析阶段直接报错，不是静默忽略 |
| 任意随包分发的文件（zip 里怎么放就怎么解出来） | `[build]` 段与 resources：manifest 里没有这两个概念，写了会被当未知字段静默丢掉 |
| — | server 的环境变量：`[[capabilities.mcp]]` 没有 `env` 字段，所以"需要凭据的 server"打不进包 |
| 根线程（跑 cell 的那个）的工具面 | 子 agent 的工具面——它跑在自己的 thread 上，拿不到包里的 server |

还有三条附带约束，都会在包跑起来之前拦下：

- `entry` 必须是 `.js` 或 `.mjs`，且必须在包内。`..`、绝对路径、指向包外的软链一律拒（canonicalize 之后再判）
- 包上限 64 MiB，因为它 base64 之后要塞进一条 JSON-RPC 消息；解压后另有 256 MiB 与 100 倍膨胀比两道闸，条目数上限 4096
- 解出来的文件权限固定为 `0644`，所以 server 要写成 `shell = "node mcp/echo.mjs"` 这样带解释器的命令行，不能依赖可执行位还在

一个声明了的 server 如果没起来，整个 cell 会被拒跑，而不是少个工具照跑。判据是"这个 server 有没有在工具面上留下工具"，所以起来了但一个工具都不报的 server 同样算缺席。报错文案：`refusing to run: required MCP server(s) unavailable: <名字>`。这是刻意与采样循环相反的选择：模型少一个工具可以改走别的路径，程序不能，它是按申报的能力写死的。

### 自带能力解决不了的：子 agent 的系统提示词

包能带工具，带不了角色。子 agent 的基础指令从父会话继承，角色层只能从宿主已装好的 `agent_type` 里选，程序唯一完全控制的是 `message`。分层与写法见《05-writing.md》§5。

写代码时的结论：`message` 要写扎实。用宿主侧的工具之前先在 `ALL_TOOLS` 里探测，对缺席路径写降级；包里自带的那些不用探测，起不来就已经拒跑了。

---

## 7. 没有启动参数

提交接口收的是线程 id 和一个包（`{ threadId, package }`），仍然没有一个放参数的地方。

包解除的是"外部数据进不来"，不是"运行时传参"：

| 能做 | 不能做 |
| --- | --- |
| 把数据文件打进包里（只有包自带的 MCP server 读得到，见下） | 同一个包按不同参数跑出不同行为 |
| 由自研客户端每次现拼包（源码里写死不同常量） | 提交时另附一个 `args` 字段 |
| 用 `-C` / `-m` 这类命令行开关 | — |

注意第一条那个括号：包里的文件确实完整解出来了，但解包路径不会以任何形式交给程序。所以"随包带数据"只有两种写法——写进 `entry` 的源码里，或者由包自带的 MCP server 去读、程序调它拿。原因与写法见《05-writing.md》§3。

---

## 8. 并发上限：两代语义不同

算法是契约，数字是快照："计累计还是计驻留""减不减 root"这两条规则不会变，6 和 4 只是配置默认值。

| | v1 | v2 |
| --- | --- | --- |
| 上限计的是什么 | 一次运行里累计派出的 agent 数 | 同时驻留的 agent 数 |
| 默认上限 | 6，root 不占名额 | `max_concurrent_threads_per_session` 减 1（root 占一个）：默认 4 → 3 |
| 跑完的 agent 释放名额吗 | 不释放，必须显式 `closeAll` | 会被自动驱逐让位 |

超订不会排队，派发直接抛异常。`concurrency` 这个参数守不住上限——它限的是同时在途的派发调用数，已派出的 agent 全都还在。

条目数超过可用名额时，唯一的写法是分批：v1 每批之后 `closeAll` 回收名额，v2 每批必须先收齐再派下一批。v2 下这是静默的：驻留位满时宿主驱逐一个已完成的 agent，它随即从 `list_agents` 里消失，而 v2 正是靠 `list_agents` 收口，于是那个结果被填成 `null`，不报错。

完整的分批写法见《05-writing.md》§6.3。

---

## 9. 递归深度：v2 下无条件放行

子 agent 能不能再派孙 agent，两代行为相反：

| 后端 | 行为 |
| --- | --- |
| v1 | 深度上限默认为 1，且拦两道——超限的子 agent 在 JS 里根本看不到那组工具，即便看到运行时还会再拦一次。递归会自己停 |
| v2 | 两道都没有。子 agent 拥有与根线程同样的工具，包括派发工具 |

v2 下的限制只能在 JS 里自行实现：传一个深度计数进 `message`，或只在顶层派发。唯一的兜底是并发上限，那是个数量闸不是深度闸，触发时已经起了大量 agent。

而后端是 v1 还是 v2 不由本地配置决定，由模型元数据决定。

---

## 10. 运行时读不到工具 schema

`ALL_TOOLS` 里只有 `{ name, description }`，没有参数 schema。

程序能知道有哪些工具，却不知道每个工具收什么字段、返回什么形状。

《07-api-reference.md》是这个缺口的离线替代品，但那是查表不是运行时能力：程序自己在 JS 里仍然读不到 schema。

还有一档更窄的情况：有些工具是 `Deferred` 曝光度，连那句描述都拿不到完整形状（`ALL_TOOLS` 里有名字和描述，但工具说明文本里没有它的声明）。v1 那 5 个子 agent 工具默认就是这一档。

---

## 11. 重跑不可复现

沙箱里有真实的 `Date.now()`，而没有任何日志能用来重放一次运行。

同一段程序重跑两次，结果可能不同：模型输出本身有随机性，加上真实时钟和外部文件系统状态，两次运行没有可比性保证。

写代码时的结论：不要把"上次跑出来是这样"当作断言依据。需要稳定判据时，把判据写成程序侧的检查（比如"它挑的那个名字是否出自它自己上一轮列的名单"），而不是比对整段输出。

---

## 12. 两件看似缺失但不需要的东西

以下两项在 PoA 的使用场景下不构成缺口，不需要照着其他系统的清单去追平：

| 看似缺失 | 为什么不需要 |
| --- | --- |
| 没有封闭的错误码体系 | 工具失败就是 JS 异常，`try/catch` 够用 |
| 没有版本协商 | `poa_api_version` 只认 `1`，对不上直接拒。在只有一个版本的时候，协商就是多一层可以谈崩的地方 |

影响"能写出什么程序"的是上面 §1–§11 那些。
