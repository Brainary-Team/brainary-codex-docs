---
title: 边界与限制
description: 硬边界清单，以及每一条在写代码时的具体样子
---

# 边界与限制

[← API 参考](./07-api-reference.md) · [返回目录](./index.md) · 下一篇：[故障排查](./09-troubleshooting.md)

**这条通道可用，但不宜高估。** 下面每一条都是当前的实际行为，不是设计意图——其中多数会随实现推进而解除，[§12](#12-这些边界里哪些会变) 逐条标出了对应的 issue。

---

## 0. 速览

| 边界 | 一句话 | 落到代码上的样子 |
| --- | --- | --- |
| [没有续跑](#1-没有续跑) | 让出之后接不回来 | 长任务只能靠调大 `yield_time_ms` 硬扛 |
| [没有取消](#2-没有取消) | 跑起来就停不下来 | 跑飞了只能杀进程；调试期规模压到 1 |
| [无人值守](#3-无人值守) | 程序中途没法向人提问 | 需要判断的地方只能写成代码 |
| [等待调用串行](#4-等待调用串行并发不等于流式) | `Promise.race` 拿不到最先完成的 | 别设计成流水线 |
| [没有结构化输出保证](#5-没有结构化输出保证) | prompt 里的格式要求只是要求 | 必须防御性解析 + 留原文 |
| [自带能力只到 stdio MCP](#6-自带能力只到-stdio-mcp) | 包能带自己的 MCP server，但带不了角色与提示词 | 能力写进 manifest；`message` 仍是唯一控制面 |
| [没有启动参数](#7-没有启动参数) | 提交接口不收参数，只收一个包 | 参数写死或打进包里，不能运行时传 |
| [并发有上限且两代不同](#8-并发上限两代算法不同) | v1 是 6，v2 实际只剩 3 | `concurrency` 按可用名额估 |
| [v2 没有递归深度刹车](#9-递归深度v2-下无条件放行) | 子 agent 能无限往下派 | 刹车自己在 JS 里踩 |
| [运行时读不到工具 schema](#10-运行时读不到工具-schema) | `ALL_TOOLS` 只有名字和描述 | 参数只能查文档 |
| [重跑不可复现](#11-重跑不可复现) | 有真实时钟，没有可回放的记录 | 别依赖两次运行结果一致 |

---

## 1. 没有续跑

程序中途让出（`yield_control()`）之后，**已累积的输出会交出去，但后半段回不来**——没有配套的手段去接。

模型侧有这个能力（它可以用一个专门的工具续跑一个让出的 cell），**程序侧没有对应物**。

**写代码时的结论**：

- `yield_control()` 不能当"流式输出"用
- 长程序的正解是把首行 pragma 的 `yield_time_ms` 调大，而不是分段
- 整个并行派发必须在这一次运行的超时内跑完

---

## 2. 没有取消

**没有任何手段能中止一个正在跑的程序。** 跑飞了只能杀进程。

配合上一条（让出后无法续跑），调试成本很高。由此产生的习惯见[编写指南 §9](./05-writing.md#9-调试循环在没有取消没有续跑的前提下)：

- `yield_time_ms` 分两档写，调试期 `60000`
- 规模常量提到顶部，调试时全设成 1
- 观测点靠攒 `log` 数组，最后随结果一起交出去

---

## 3. 无人值守

**程序没有跟人对话的手段。**

内置的 `request_user_input` 有三道门，**每一道单独就足以挡死**：它挂在一个实验开关后面；注册时的曝光度是 `DirectModelOnly`，而这一档**判定为不进 code mode**；即便前两道都放开，它还会拒绝非根线程的调用。三道是并列的，不是调一处配置能解决的。

> [!NOTE]
> **这一条正在被处理，是当前实现的状态而不是设计终点。**
> 对应 **issue #21**，正在推进，尚未落地。
> 但有一条与最终方案无关、现在就该知道的边界——**`codex exec --poa` 这条路问不到人**：`codex exec` 在 `ConfigOverrides` 里把审批策略设成 `never`，这一层压在 `-c` 之上、覆盖不掉；问人的请求于是一律在毫秒级被自动 decline，客户端全程收不到。**打成包分发的程序，即使将来这条落地了也问不到人。**

**在它落地之前，写代码时的结论**：

- PoA 程序必须能在没有人的情况下从头跑到尾
- 需要人判断的地方，只能把判断也写成代码（比如[模式库](./06-patterns.md)里交叉验证的数票），或者把问题留到最后的输出里
- 整条链路上不存在任何人工审批点——任何一次阻塞询问都会把它挂死

---

## 4. 等待调用串行（并发不等于流式）

工具执行时会取一把共享锁；**声明了并行安全的取读锁（可共存），没声明的取写锁（独占）**。子 agent 那两代工具**一个都没有声明并行安全**。

> [!IMPORTANT]
> **agent 本身是真并发的**——三个同时干活，总耗时接近最慢那个而不是三者之和。
> **串行的是程序侧的等待调用。**

**丢掉的具体是这三件事**：

| 做不到 | 具体表现 |
| --- | --- |
| 流式输出 | 没法边收边处理 |
| 早停 | 拿到满意结果后没法提前结束剩下的 |
| 动态调度 | 等待期间连再派一个新 agent 都做不到 |

**写代码时的结论**：按「全部派出去 → 一次 join → 统一收口」组织。

**唯一不改上游的绕法**：把重活放进一个**标了只读的 MCP server**——那样它就是并行安全的（判据见 [API 参考 §3.1](./07-api-reference.md#mcp5)）。

> [!TIP]
> **这条绕法不需要宿主配合。** 包自带的 stdio server 也走同一条判据——「工具自己声明并行安全**或**带只读标注」。包这边走不通前半条（`ext/poa` 建的 server config 里 `supports_parallel_tool_calls` 硬编码为 `false`，**服务器级的整体豁免用不了**），所以只能**逐个工具标只读**。
>
> 两个附带前提别漏：`destructiveHint: true` 会**先短路**掉只读判定，三个标注要一起写；被 tool_search 藏起来的工具（`Hidden` 曝光度）也不算并行安全。写法见[核心概念 §9](./03-concepts.md#9-poa-包自带能力的那条路)。

---

## 5. 没有结构化输出保证

**没有"内核校验 JSON schema、不合规就重试"的机制。** prompt 里那句 `只回一行 JSON` 只是**要求**，不是**保证**。

**写代码时的结论**：

- 每个 agent 回复都过 `parseJsonReply`
- **永远处理 `error` 分支**
- 解析失败时把原始回复截前 200 字留下来——否则只能知道"失败了"，无从判断它到底回了什么

顺带一提：`succeeded / total` 这个比值量的**不是 agent 答得好不好，而是这套替代方案有多不靠谱**。

---

## 6. 自带能力只到 stdio MCP

> [!NOTE]
> **程序可以自带能力。** 把它打成一个 `.poa` 包，在 `manifest.toml` 里写 `[[capabilities.mcp]]`，服务端解包后按 thread 起这些 server，随 thread 生死，不写全局 `config.toml`。写法见[核心概念 §9](./03-concepts.md#9-poa-包自带能力的那条路)。

**这一条限制的不是"能不能自带"，而是"自带到什么程度"。** 当前的边界：

| 能自带 | 不能自带 |
| --- | --- |
| `stdio` 传输的 MCP server（进程直接派生，不经 shell） | `network` 传输——**解析阶段直接报错**，不是静默忽略 |
| 任意随包分发的文件（zip 里怎么放就怎么解出来） | `[build]` 段与 resources：manifest 里**根本没有这两个概念**，写了会被当未知字段静默丢掉 |
| — | server 的环境变量：`[[capabilities.mcp]]` **没有 `env` 字段**，所以"需要凭据的 server"暂时打不进包 |
| 根线程（跑 cell 的那个）的工具面 | **子 agent 的工具面**——它跑在自己的 thread 上，拿不到包里的 server |

还有三条附带约束，都会在包跑起来之前就把你拦下：

- **`entry` 必须是 `.js` 或 `.mjs`，且必须在包内**——`..`、绝对路径、指向包外的软链一律拒（canonicalize 之后再判，绕不过去）
- **包上限 64 MiB**，因为它 base64 之后要塞进一条 JSON-RPC 消息；解压后另有 256 MiB 与 100 倍膨胀比两道闸，条目数上限 4096
- **解出来的文件权限固定为 `0644`**——所以 server 要写成 `shell = "node mcp/echo.mjs"` 这样带解释器的命令行，**不能依赖可执行位还在**

> [!WARNING]
> **一个声明了的 server 如果没起来，整个 cell 会被拒跑，而不是少个工具照跑。**
> 判据是"这个 server 有没有在工具面上留下工具"——所以**起来了但一个工具都不报的 server 同样算缺席**。
> 报错文案：`refusing to run: required MCP server(s) unavailable: <名字>`。
> 这是刻意与采样循环相反的选择：模型少个工具可以换条路走，程序不行，它是照着申报的能力写死的。

### 自带能力解决不了的：子 agent 的系统提示词

**设不了。** 派发接口的参数面上根本没有这一项，而且 v2 那边会拒绝未知字段——自作主张塞一个**不会被静默忽略，而是直接抛异常**。

| 层 | 来源 | 程序能否控制 |
| --- | --- | --- |
| 基础指令 | 从父会话继承 | ❌ |
| 角色层（模型、推理强度、工具集） | `agent_type` 指向的角色配置 | ⚠️ 只能**选**宿主已装好的，不能现场定义 |
| 任务内容 | `message` | ✅ 完全控制 |

> `agent_type` 是一个**角色名，不是一段 prompt**。传一个宿主没配过的名字会直接报错。

**写代码时的结论**：`message` 是程序唯一的控制面，要写扎实（三件事的写法见[编写指南 §5](./05-writing.md#5-写子-agent-的任务文字)）。用宿主侧的工具之前先在 `ALL_TOOLS` 里探测，对缺席路径写降级；**包里自带的那些不用探测——起不来就已经拒跑了**。

---

## 7. 没有启动参数

提交接口收的是线程 id 和**一个包**（`{ threadId, package }`），**仍然没有一个放参数的地方**。

包解除的是"外部数据进不来"，不是"运行时传参"：

| 能做 | 不能做 |
| --- | --- |
| 把数据文件打进包里（**只有包自带的 MCP server 读得到**，见下） | 同一个包按不同参数跑出不同行为 |
| 由自研客户端**每次现拼包**（源码里写死不同常量） | 提交时另附一个 `args` 字段 |
| 用 `--cd` / `--model` 这类命令行开关（demo 那条路上是 `--cwd`，见[编写指南 §13 ⑤](./05-writing.md#13-从-js-到-poa要改的五件事)） | — |

注意第一条那个括号：包里的文件确实完整解出来了，但解在服务端的一个临时目录里，**这个路径不会以任何形式交给程序**。症结是"拿不到路径"而不是"没有读的手段"——`exec_command` 走宿主进程，能读整个文件系统，但它的相对路径基准是 `--cwd`，不是包目录。

**于是"随包带数据"只有两种写法**：写进 `entry` 的源码里（最直接），或者由包自带的 server 去读、程序调它拿（它的 cwd 就是包根）。

详见[编写指南 §3](./05-writing.md#3-外部输入怎么进到程序里)。

---

## 8. 并发上限：两代算法不同

**算法是契约，数字是快照**——「减不减 root」这条规则不会变，而 6 和 4 是配置里的默认值，宿主改了就不是这个数。

| 后端 | 可用子 agent 名额 | 默认值 |
| --- | --- | --- |
| **v1** | 配置里那个数，**不减 root** | **6** |
| **v2** | 配置里那个数 **减 1**（root 线程占一个名额） | 上限默认 4 → **实际只剩 3** |

**同一段程序换个 profile，能开的并发数就从 6 变成 3。**

> [!WARNING]
> **超订不会排队，派发直接失败。** `concurrency` 要按可用名额估，保险起见按「上限减 1」取。

另外 **v1 下已完成的 agent 不关掉仍然占着名额**——这就是 `closeAll` 不能省的原因。

---

## 9. 递归深度：v2 下无条件放行

子 agent 能不能再派孙 agent，两代行为**相反**：

| 后端 | 行为 |
| --- | --- |
| **v1** | 深度上限默认为 1，且拦两道——超限的子 agent **在 JS 里根本看不到那组工具**，即便看到运行时还会再拦一次。递归会自己停 |
| **v2** | **两道都没有。** 子 agent 拥有与根线程同样的工具，包括派发工具 |

> [!WARNING]
> **v2 下刹车只能在 JS 里自己踩**——传一个深度计数进 `message`，或干脆只在顶层派发。
>
> 唯一的兜底是 agent **总数**上限，那是个数量闸不是深度闸，**触发时已经起了一堆 agent**。
>
> 而后端是 v1 还是 v2 **不由本地配置决定**（由模型元数据决定），所以"本地跑的是 v1，不用管"这个假设不成立。

---

## 10. 运行时读不到工具 schema

`ALL_TOOLS` 里只有 `{ name, description }`，**没有参数 schema**。

程序能知道有哪些工具，却不知道每个工具收什么字段、返回什么形状。

[API 参考](./07-api-reference.md) 是这个缺口的**离线替代品**——但那是查表，**不是运行时能力**：程序自己在 JS 里仍然读不到 schema。

还有一档更窄的情况：有些工具是 `Deferred` 曝光度，**连那句描述都拿不到完整形状**（`ALL_TOOLS` 里有名字和描述，但工具说明文本里没有它的声明）。**v1 那 5 个子 agent 工具默认就是这一档。**

---

## 11. 重跑不可复现

沙箱里有真实的 `Date.now()`，而**没有任何日志能用来重放一次运行**。

**同一段程序重跑两次，结果可能不同**——模型输出本身有随机性，加上真实时钟和外部文件系统状态，两次运行没有可比性保证。

**写代码时的结论**：别把"上次跑出来是这样"当作断言依据。需要稳定判据时，把判据写成程序侧的检查（比如"它挑的那个名字是否出自它自己上一轮列的名单"），而不是比对整段输出。

---

## 12. 这些边界里，哪些会变

上面多数条目是**当前实现进度的产物**，不是设计意图。带编号的那几条各自有一个在推进的 issue：

| 边界 | 落地之后会变成什么 | issue |
| --- | --- | --- |
| §1 没有续跑 | 程序侧有手段接回让出的 cell | **#15** |
| §2 没有取消 | 取消句柄提到 RPC 层，能中止正在跑的 cell | **#16** |
| §3 无人值守 | 程序问得到人。**但只对运行器那条路成立**——`codex exec` 在 `ConfigOverrides` 里把审批策略设成 `never`，压在 `-c` 之上覆盖不掉，所以 `.poa` 分发的程序仍然问不到人 | **#21** |
| §4 等待调用串行 | 派发真正并行，流式 / 早停 / 动态调度都成立 | **#17** |
| §5 没有结构化输出保证 | 有输出校验与失败重试 | **#14** |
| §10 读不到工具 schema | schema 传进 JS，运行时能读 | **#13** |
| §11 重跑不可复现 | **只解决一半**：目标是"事后查得到这次跑了什么"，不是"能重放" | **#24** |
| `store` / `load` 模型看不见（没单列成节，写在[核心概念 §3](./03-concepts.md#3-沙箱面有什么没什么)与 [API 参考 §1](./07-api-reference.md#1-全局-primitive12-个)） | 程序与子 agent 读写同一张短期记忆表 | **#19** |
| §6 的"不能自带"那一列 | 包能带 `network` MCP、`[build]`、resources | — |
| §6 子 agent 拿不到包里的 server | 子 agent 继承包申报的能力 | — |
| §7 没有启动参数 | 提交时能另附参数 | — |

**按当前行为写代码。** 真要依赖其中某一条时，先去主仓 issue 列表看一眼状态——这张表不会自己变。

---

## 13. 哪些"缺口"其实不是缺口

有几件事看起来像缺，但对 PoA 的使用场景来说本来就不需要——照着别的系统的清单去追平是错的：

| 看起来缺 | 为什么其实不需要 |
| --- | --- |
| 没有封闭的错误码体系 | 工具失败就是 JS 异常，`try/catch` 够用 |
| 没有版本协商 | `poa_api_version` 只认 `1`，对不上直接拒。在只有一个版本的时候，协商就是多一层可以谈崩的地方 |

> [!IMPORTANT]
> **往这张表里加东西之前，先确认理由不是"反正现在也做不到"。**
> 上面两条的理由都与实现进度无关：错误码体系是因为 JS 本来就有异常，版本协商是因为只存在一个版本。
> 一旦理由变成"当下恰好没有"，那它就不属于这一节，而属于 [§12](#12-这些边界里哪些会变)。

**真正的缺口**是上面 §1–§11 那些——它们影响的是**能写出什么程序**。

---

[← API 参考](./07-api-reference.md) · [返回目录](./index.md) · 下一篇：[故障排查](./09-troubleshooting.md)
